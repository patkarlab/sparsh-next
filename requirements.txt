# SPARSH-next runtime dependencies (no new packages relative to v0.1.0; Optuna and seaborn are no longer needed).
# Install the PyTorch build that matches your CUDA toolkit. On the lab server the simplest route is to
# clone the existing working environment (see docs/SETUP.md) rather than installing anything.
torch>=2.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
